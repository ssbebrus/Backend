version: '3'

volumes:
  pg_data:

services:
  db:
    image: postgres:14
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - db